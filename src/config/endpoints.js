const publishedContent = "https://deliver.kenticocloud.com";
const unpublishedContent = "https://preview-deliver.kenticocloud.com";

const endpoints = {	
  publishedContent,
  unpublishedContent
}

export default endpoints
export { publishedContent, unpublishedContent }
